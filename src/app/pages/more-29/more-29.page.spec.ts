import { ComponentFixture, TestBed } from '@angular/core/testing';
import { More29Page } from './more-29.page';

describe('More29Page', () => {
  let component: More29Page;
  let fixture: ComponentFixture<More29Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(More29Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
